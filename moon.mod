// Learn more about moon.mod configuration:
// https://docs.moonbitlang.com/en/latest/toolchain/moon/module.html

name = "kosei28/maru"

version = "0.3.0"

readme = "README.mbt.md"

repository = "https://github.com/kosei28/maru"

license = "MIT"

keywords = [ "json", "validation" ]

description = "A minimal MoonBit validation library that constructs a typed value while validating, without holding a schema as a value."

source = "src"
